/* function ff(arr,str) {
     arr.map((obj) => obj.continent = str)
    return arr
 }
  rceat native application
  
 console.log(ff([{ city: 'Jeddah', country: 'Saudi arabia' }, { city: 'Dubai', country: 'UAE' }], 'Asia'))
*/

function todo(arr,str){

const er =`hi ${arr},\n how are you? \n are you ${str} yet? Yes Iam ${str} old`
console.log(er
    er.localeCompare('we')
}

try {
   todo("Faisal",15);
}catch (e) {
    console.log(`${e}`)
}
console.log(`program counted\nop/`)
